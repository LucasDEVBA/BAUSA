{
  "name": "bolsa-atleta-usa",
  "private": true,
  "scripts": {
    "dev:web": "turbo dev --filter=@bolsa-atleta/web",
    "dev:engine": "turbo dev --filter=@bolsa-atleta/engine",
    "dev": "turbo dev",
    "build": "turbo build",
    "build:web": "turbo build --filter=@bolsa-atleta/web",
    "build:engine": "turbo build --filter=@bolsa-atleta/engine",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.30.3"
}
